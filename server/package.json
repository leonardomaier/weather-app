{
  "name": "server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch-path=src --env-file-if-exists=../.env src/index.js",
    "start": "NODE_ENV=production node --env-file-if-exists=../.env src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
